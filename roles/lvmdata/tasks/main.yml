---

# install dependencies and rescan bus for new disks.
- include: debian.yml
  when: ansible_os_family == "Debian"

- include: redhat.yml
  when: ansible_os_family == "RedHat"

# Setup LVM /data partition according to configuration
- include: manage_lvm.yml
